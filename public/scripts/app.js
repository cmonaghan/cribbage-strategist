// Generated by CoffeeScript 1.7.0
var Card, Hand, cardsDealt, fourCardArr, i, j, possibleHands, scoreFifteens, scoreFlush, scoreHand, scoreNobs, scorePairs, scoreRuns, suit, twoTossedArr, value;

Card = function(value, suit) {
  this.value = value;
  this.suit = suit;
  return this.scoreValue = value;
};

Hand = function(fourCardArr, twoTossedArr) {
  this.cardsKept = fourCardArr;
  this.cardsTossed = twoTossedArr;
  return this.expectedValue = 0;
};

cardsDealt = [];

i = 0;

while (i < 6) {
  value = random(13);
  suit = random(4);
  cardsDealt.push(new Card(value, suit));
  i++;
}

console.log(cardsDealt);

possibleHands = [];

i = 0;

while (i < cardsDealt.length) {
  j = 0;
  while (j < cardsDealt.length - 1) {
    fourCardArr = cardsDealt.slice();
    twoTossedArr = [];
    twoTossedArr.push(fourCardArr.splice(i, 1));
    twoTossedArr.push(fourCardArr.splice(j, 1));
    possibleHands.push(new Hand(fourCardArr, twoTossedArr));
    j++;
  }
  i++;
}

console.log(possibleHands);

scoreHand = function(hand) {
  var score;
  score = 0;
  score += scoreFifteens(hand);
  score += scorePairs(hand);
  score += scoreRuns(hand);
  score += scoreFlush(hand);
  score += scoreNobs(hand);
  return hand.score = score;
};

scoreFifteens = function(hand) {};

scorePairs = function(hand) {};

scoreRuns = function(hand) {};

scoreFlush = function(hand) {};

scoreNobs = function(hand) {};
